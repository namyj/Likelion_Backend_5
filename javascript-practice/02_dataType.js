// 정수
const len = 10
const size = 5

// 실수
const temp = 36.5
const dis = 3.5
const floatPoint = 2.99e8

// 무한대
const infinit = Infinity
const infNegative = -Infinity

// not a Number (NaN)
// 숫자가 표현되어야 하는 계산 도는 함수의 결과가 숫자가 아닐 때
const parseFail = parseInt("A")
const sqrtNegative = Math.sqrt(-1)
const cacul = 7 + NaN

// String
const single = 'hello'
const double = "hello"
const concat = single + double
console.log(concat)
const paragraph = 'this \nis \nnewline'
console.log(paragraph)

// Backtick (`)을 사용해 Template Literal을 만들 수 있다.
const name = 'nam'
const message = `To ${name}, hello!!`
console.log(message)


// Boolean

// null & undefined